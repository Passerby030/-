**第五次笔记（LED流水灯）**

流水灯实现逻辑：“通过延时函数控制不同的IO口，使得灯能一盏一盏的亮下去，就像流水一样”

IO口批量控制：”通过给IO直接赋值快速批量控制”

延时函数：“可以给延时函数添加参数，修改其循环秒数，从而方便快捷地控制灯的运行”如下

void delay_ms(unsigned int ms)
{
	unsigned int i,j;
	while(ms)
	{
	for(i=1;i>0;i--)
	 for(j=110;j>0;j--);
	
	ms--;}
	}