**第四次笔记**（LED闪烁）

阻塞延时函数”“通过使CPU进行空循环和特定计时状态来停止执行任何有用的任务”

LED闪烁：通过亮灭的循环实现两次切换

关键：“通过延时函数使得亮与灭期间有间隔”

（INTRINS.H）的头文件需要声明用于执行延时函数

模型为：while();

​	{

​	亮

​        延时函数

​	灭	

​	}

CPU时钟：‘’每响一下，CPU执行一个最小动作”（xMHz为一秒响x百万次）

CPU时钟周期：“CPU每响一次的最小时间单位”



